[x] remove all styles which are defined and use latest tailwind v4 version of implementing them if they are not dynamic of course like shadows 
    if there is complex styles let me know them maybe it is better to use custom utility in global css
[x] check testimonials section progress bar behavior should be when user  is not doing anything then it should
    show progress bar that slides will change soon, if user makes any action like changing or draggin slides then instead of proggress bar we should show
    full version of dot and stop auto play for a while like x amount of time, then slide to next slide and start progress again, same if user will 
    scroll or if section is invisible and goes visible again
[x] in popup or menu I expect the header to be under fogged bg and hide cross button (maybe even remove it leave it as burger) 
[x] if use exact same style in both popup and menu we can create utility in global.css (I adjusted menu styles to make it exact as in design) or create reusable component
[x] nav links and menu links are not navigating to exact section we should use lenis and maybe custom hook to make it smooth navigation to needed sections , also check over app if there is any inner page link (anchored link)
[x] with all optimisations I made animations still look glitchy and heavy.. it should be smooth so add optimisation classes like will change or   transform: translateZ(0);
  backface-visibility: hidden;
  will-change: transform;
  or any other extra things could be done to optimise them. I dont think we could need requestAnimation frame as most of animations are css based or framer which already does it
[x] button component with glass effect should be complete new component so it is always small or we can make current version of it much smaller here is desktop version styles for glass button
/* Button glass*/

display: flex;
flex-direction: row;
justify-content: center;
align-items: center;
padding: 8px 16px;
gap: 10px;
background: linear-gradient(160.98deg, rgba(255, 255, 255, 0.075) 7.11%, rgba(254, 243, 139, 0.135) 42.81%, rgba(254, 243, 139, 0.15) 67.04%, rgba(255, 255, 255, 0.03) 95.51%), rgba(255, 255, 255, 0.01);
box-shadow: 4px 11px 11px rgba(0, 0, 0, 0.05), inset -0.5px 0.5px 1.5px rgba(255, 255, 255, 0.77), inset 0.5px -0.5px 1px rgba(0, 0, 0, 0.6), inset 0px 4px 9.4px rgba(255, 255, 255, 0.25);
border-radius: 16px;

font-family: 'Manrope';
font-style: normal;
font-weight: 600;
font-size: 18px;
line-height: 25px;
text-align: center;
letter-spacing: -0.02em;
color: #2B2929;
--- mobile version is same 


[x]  in mobile view completely remove paggination for testimonials section and remove functionality or stop it to handle performance leak
