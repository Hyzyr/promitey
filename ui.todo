[-]     lets fix pricing, it should be
        monthly : 3eur,
        year: 16eur,
        half year: 12eur
        three month : 7.5 eur.
        calculate discount and what original price (overlined as previous price) . you can round original price and leave 2 decimals after document

[-]     update guide section to have 3 different image in each step
        public/images/guide/item01.png
        public/images/guide/item02.png
        public/images/guide/item03.png
[-]     not ui but in this todo, when user create account I get feedback that it is not always redirects user to dashboard. check why it could be.
[-]     next in api /me we have usedTrial info which is letting know if client has used trial period or not, so on dashboard main page where we show
        subcription we should also show try trial version kind of button, and it should send promocode TRIAL7DAY automatically to backend
        при клике фронт отправляет TRIAL7DAY на эндпоинт /promocode/activate
[-]     /vpn/openvpn/config/{region} for region-specific download in swagger-new.json:693
        implement openvpn by regional download should be exiting already in openvpn modal but should be commented
[-]     also we have to remove everything from link telegram section (aslo make sure tilt of this card consistent with others)
        and leave only one button "привязать аккаунт":
        теперь весь линкинг происходит через post /link/site-token
[-]     landing pricing mobile: when clicking a pricing card opens modal, closing modal scrolls page to top. It should keep the same scroll position.
