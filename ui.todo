[-]     fix link telegram button in dashboard, it shows link account button and after user click it it changes UI styles and gets wide as full width of card, 
        it is not clear for end user, we should keep styling and instead change button to secondary with different text, like open in telegram
[-]     fix subscription card, backend says:
        обновил апи, теперь вместо целой строки из бд возвращается одно из трех
        "enum": ["tribute", "stars", "activation_code"]

        для stars и activation_code отображаем end date

        для tribute - нет, мы не знаем точной даты окончания подписки из-за особенности рекуррентной подписки трибьют

        не отображать эти строки as is: tribute - Telegram Tribute, stars - Telegram Stars, activation_code - Код активации

[-]     in dashboard lets create rule all buttons should be alligned on left if not in one row with input, 
        ex: for buttons, try 7 days free, reset subscription, recreate config.
        btw : lets fix typo for try 7days free, lets make it something like "try for free" or "activate trial"
[-]     update typo, make it more relatevie what app dashboard can do right now:
        "Manage access, monitor client activity and keep an eye on server health — all in one place."
[-]     benefits section  > your internet card > try for free button does nothing, make it same behaviour as others
        if not logged in move to login page if logged in hide it or move to dashboard
[-]     dashboard> change password card (only in card) remove text:
        "For security, password changes continue from the auth flow after signing out of this session.",
        we have it in popup modal
[-]     nav link in header and menu of landing page does not work on legal pages. (all pages that are not landing page itself)
[-]     when loggin in after successfull sign in page does not moves you to dashboard page some why. can you check it 
        either give correct message to user what happened or redirect properly? check that user is not clicking sign in button again and agin by accidently. and not time 
        between preloader shown and redicrecting to make button active again for miss clicks..
        
         
