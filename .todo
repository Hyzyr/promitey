[-] 	hey first we need to plan how to make lenis smooth scroll only work if there is a env variable saying we need smooth scroll, 
		it rises question from me, will it still load the lenis plugin and make humble for performance? 
[-] 	we need plan on new swagger.json update check what is updated from previouse swagger and plan for implemeting it properly.
		client says:
		заодно нужно прикрутить TOTP, в апи доке есть
		POST /auth/totp/setup
		POST /auth/totp/enable
		POST /auth/totp/disable
		tell  me what is TOTP? and is it all ready for it to be implemented? 
		---- 
		client also want to add promocode input, we have it in app already just uncomment it and check swagger docs if it works properly with new update
		разрешённые символы: A-Z a-z 0-9 . _ + -
		длина: 1..64
		whitespace вокруг trim
		add this validation to promocode input.
		